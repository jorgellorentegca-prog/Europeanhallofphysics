## Packages
framer-motion | Animations for page transitions and elements
react-simple-maps | For the interactive Europe map
d3-scale | Helper for map coloring
react-tooltip | Tooltips for the map
clsx | Class name utility (if not already installed)
tailwind-merge | Class merging (if not already installed)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
